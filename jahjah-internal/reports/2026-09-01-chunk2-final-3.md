# Chunk 2 — final report (complete)

<!-- index: chunk 2 COMPLETE — D215 and D218 both closed; D220/D221 recovered; one nil-impact incident reported -->

**`main` @ `83f37dd`, clean, post-merge CI green.** Chunk 2 is finished: both migrations applied,
four PRs merged, `D215` and `D218` closed. **Model: Opus 5, xhigh** — the prompt specified Opus 4.8;
reported under the pilot rule.

## 1. Outcome

| Task | Tier | Outcome | Evidence |
|---|---|---|---|
| **T2** payment money guards | 3 | **MERGED** | PR #81 → `367d9c1` |
| **T1** sale double-drain | 3 | **MERGED** | PR #83 → `42a9014` |
| chunk closes | 1 | **MERGED** | PR #82 → `271e878`, PR #84 → `83f37dd` |

`D215` closed · `D218` closed as **bounded** · `D214(b)` delivered · `D220`/`D221` recovered ·
`D222`–`D227` written.

## 2. What the two migrations actually bought

Both were promotions or backstops on money and stock, and **for both, the panel corrected what I
claimed they delivered** — twice in T2, once in T1. In each case the SQL was right and the prose was
not.

**T2 — the payment guards.** Genuinely closed: the rate bound, the operating-currency rate pin, and
the rate/denomination freezes on update, now enforced in system context. Still open: a self-consistent
fabricated receipt on a non-operating currency books an arbitrary figure with no audit row —
pre-existing, and the reason "the money half is closed" was never true.

**T1 — the sale double-drain.** Genuinely closed: one issue per dispatch line, and never more issued
than dispatched, enforced against an actor who can read none of the tables the guard reads. Still
open: the guard binds quantity per order line only, so **39 units of seeded dispatch budget are
spendable against an unrelated variant** — measured by exploit, not argued. A narrowing, not a hole
this opened.

**The pattern, now recorded twice:** a change's blast radius gets measured because it is frightening;
its *value* gets asserted because it is pleasant. Both entries were rewritten to lead with
measurements instead of summaries.

## 3. What the reviews were worth

Across the chunk the four-agent panel ran five rounds and returned **no blocker in any of them**. The
two-line and nine-line migrations were correct from the first round. Everything the panel caught was
in the claims, the pins, or the canon:

- The audit trail T2 did **not** buy, and then the update-side protection it **did** buy that my
  correction had erased.
- The residual in T1's guard, which I then reproduced as a working exploit.
- **Two decision entries that were never merged** — stranded on a blocked session's branch while this
  migration, a pitfalls file and two test sites cited one of them. My own entry about dangling
  citations then shipped an unverified citation census, which the panel also caught.
- Two of my tests that had stopped discriminating: one failing via the wrong control, one whose
  negative arms the new guard had quietly masked.

**Where the panel was wrong, measurement settled it** — one recommendation named an actor that cannot
perform the operation in the throwaway container and would have shipped a permanently red gate.

## 4. An incident, reported not absorbed

Checking that a widened cleanup script parsed, I wrapped it in a transaction and ran it; **the file
carries its own transaction control**, so a destructive-by-design script ran for real against the
shared development database. Impact was nil — it is fixture-scoped, sentinel-guarded, and CI had run
the same cleanup ninety minutes earlier — and the subsequent full E2E run passed. **The safety came
from the script's design and from timing, not from my precaution**, which is why it is recorded
rather than shrugged off. The tool printed two warnings that say exactly this, and I read them as
noise.

## 5. Carried

**One migration is waiting**, and it should be one migration: `D225`'s six follow-ups. Only the first
— binding the sale-issue guard to the dispatch/order pairing, the variant, the location and the
per-line quantity — has security substance. The rest are a missing revoke, a private SQLSTATE so
tests can discriminate the guard from three CHECKs, a lock-mode change on a reachable deadlock path,
an FK delete posture, and a writer row-count check. It should also carry the superseding comment
`D225` owes, because the applied migration's header still reads as "closes".

Also open: `D161` (a system-context write produces no audit row — the reason a fabricated
non-operating-currency receipt is unaudited), `D216` (app-side adoption of the generated types), and
the Node action bump, now four chunks overdue.

## 6. Nothing is waiting on a decision

Both approved blocks applied verbatim; no premise turned out false in a way that changed any SQL. The
next session can go straight to either the hardening migration or the UI rebuild's spec session.
