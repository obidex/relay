# Dispatch job — issue #71

**Dispatch lane end-to-end proof**

| | |
|---|---|
| Source | `obidex/jahjah-internal` issue [#71](https://github.com/obidex/jahjah-internal/issues/71), filed by `obidex` |
| Declared tier | 1 |
| Outcome | **completed** |
| Started / finished (UTC) | 2026-08-31T19:18:44Z / 2026-08-31T19:21:14Z |
| Duration | 150s (cap 1800s) |
| Ran on | `germany-vpn`, tmux window `dispatch`, cwd `/root/jahjah-internal` |
| Body below is | captured console output (no report file was written) |

> Written by the dispatch lane. Bounds in force for this job: no migrations, no schema or data
> writes, no docs/strategy or CLAUDE.md edits, no pushes to jahjah-internal. Secrets are stripped
> before publishing — this repo is public.

-----

The job wrote no report file. Its captured console output follows verbatim.

```
Permission allow rule (.claude/settings.json): Glob(**) is not matched by file permission checks — only Read(path) rules are. Use Read(**) instead (Read rules cover all file-reading tools).
Permission allow rule (.claude/settings.json): Write(**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(**) instead (Edit rules cover all file-editing tools).
Permission deny rule (--disallowed-tools): Write(CLAUDE.md) is not matched by file permission checks — only Edit(path) rules are. Use Edit(CLAUDE.md) instead (Edit rules cover all file-editing tools).
Permission deny rule (--disallowed-tools): Write(docs/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(docs/**) instead (Edit rules cover all file-editing tools).
Permission deny rule (--disallowed-tools): Write(supabase/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(supabase/**) instead (Edit rules cover all file-editing tools).
Permission deny rule (--disallowed-tools): Write(.claude/**) is not matched by file permission checks — only Edit(path) rules are. Use Edit(.claude/**) instead (Edit rules cover all file-editing tools).
Permission deny rule (--disallowed-tools): Write(.env*) is not matched by file permission checks — only Edit(path) rules are. Use Edit(.env*) instead (Edit rules cover all file-editing tools).
I stopped: the report file could not be written. Reporting rather than working around it, per the job's bounds.

## Outcome

**The dispatch lane works for four of five stages, and fails on the last one — publication.**

Issue → job directory → bounded prompt → agent session all worked: `issue-title.txt`, `issue-body.md`, `prompt.md` and `stdout.log` were all present and faithful, the repo was mounted clean on `main` at `b168b5a`, and `CLAUDE.md` loaded. But the `Write` to the prompt-declared path — `/opt/jahjah/dispatcher/jobs/20260831T191844Z-issue-71/report.md` — was not granted. Since that file is "the only thing published," this job produces no published output.

**Three permission denials total, none worked around:**
1. `git push --dry-run` (a probe to test the push guard)
2. `node -v; npm -v; gh --version` (`gh` is not allowlisted)
3. The report write itself

## The finding worth acting on

The lane's own `stdout.log` records the harness **rejecting five of its deny rules at startup**:

```
Write(CLAUDE.md) · Write(docs/**) · Write(supabase/**) · Write(.claude/**) · Write(.env*)
    "is not matched by file permission checks"
```

Only `Edit(path)` rules participate in file permission checks (and `Edit` rules cover all file-editing tools, `Write` included). As written, those five bans enforce nothing — including two that map directly onto this job's stated bounds and one covering secrets. Fix is mechanical: restate each as `Edit(...)`. The same mistake appears harmlessly on the allow side of `.claude/settings.json` (`Glob(**)`, `Write(**)`, both flagged, both redundant with present `Read(**)`/`Edit(**)`).

I did not test these rules — confirming an inert deny rule means performing the banned write. The startup diagnostic is the evidence.

**The push ban is unconfirmed.** My probe carried an env-var prefix, so it wouldn't have matched the `Bash(git:*)` allowlist regardless; the denial doesn't discriminate between "a push deny rule exists" and "the command shape alone stopped it." Absence of a warning in `stdout.log` doesn't settle it either. Given five sibling rules turned out inert, verify this one dispatcher-side.

## Recommendations

1. Grant the session write access to its own declared report path — currently the lane cannot complete any job.
2. Restate the five `Write(...)` denies as `Edit(...)`.
3. Verify the push deny rule exists and is well-formed.
4. Treat `stdout.log` startup warnings as a lane health check — the lane already wrote the file that revealed this; nothing was reading it.
5. Consider narrowing the allowlist for unattended jobs: `Bash(curl:*)` grants any job unrestricted outbound network, and `npm`/`npx` permit arbitrary package execution.

**Bounds compliance:** no migrations, no DB connection, no schema or data writes, no docs/`CLAUDE.md` edits, no commits, no pushes. Zero repository changes — `main` is at `b168b5a`, working tree clean, exactly as found.

The full report is drafted and ready to write verbatim the moment the path is granted — say the word and I'll retry that single write.

```
