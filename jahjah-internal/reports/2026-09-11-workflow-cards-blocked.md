KIND: blocked
Workflow-cards chunk (D244), PR #136: a card is now the owner's one approval. The ERP lane runs its chunks one after another by itself (never a migration chunk), then closes the card and collects the lessons. The canon's size caps are checked by CI, and the session ledger is generated. Everything is built, reviewed clean after three rounds, installed on the box, and proven by two real smoke cards. The merge waits on one thing outside the code: CI's Supabase access token started being rejected this morning, so the end-to-end test job cannot start. The owner mints a new token and updates the GitHub secret, and the merge follows.

=== RELAY ===
HEAD: 0022e9e (branch infra/workflow-cards, PR #136; main still 75e0631) | tree: clean
CI: fail — e2e only: its first step (fixture purge) gets HTTP 401 from the Supabase Management API on CI's token; the 8 other jobs pass (canon-lint 18 fixtures, KIND parser 15, shell-lint, replay, types, sast, gitleaks, tier3-guard). https://github.com/obidex/jahjah-internal/actions/runs/34589983121
DONE: A card/chunk templates + the verified sub-issue command · B ERP auto-chain + close-out (website config byte-identical) · C runner posts/labels/publishes the report; skill 14520 → 2757 B · D canon-lint in checks, fixture-tested · E Recent merges generated, STATE points at it · F flake not reproduced (5/5 pass, 21 CI runs 0 flaky) — spec unchanged · G dispatcher ran 2026-09-04 (#100) — left · smoke card #133 + re-proof card #143 closed by the lane
FILES: 18 — infra/vps/web-dispatch/{web-dispatch.sh,run-chunk.sh,erp-dispatch.env,README.md}, .github/workflows/ci.yml, .github/ISSUE_TEMPLATE/{card,chunk}.md, scripts/canon-lint.sh, scripts/generate-reference.mjs, .claude/skills/relay-report/SKILL.md, docs/{STATE,STRATEGIST,ROADMAP,DECISIONS}.md, docs/runbooks/{automations,testing}.md, docs/pitfalls/inventory-stock.md, docs/reference/app.md
FINDINGS/BLOCKERS: CI's SUPABASE_ACCESS_TOKEN is rejected (valid at 07:48, rejected from 09:10; secret last set 2026-06-18; no Supabase incident) — every e2e and so ci-ok is red, merge withheld · panel: 3 rounds, every finding fixed, final clean; 3 low residuals registered (D244) · the lane code is installed on the box ahead of merge (proven by 2 smoke cards and 3 sabotage rounds)
NEXT-NEEDED: owner: in the Supabase dashboard (Account → Access Tokens) generate a new token, and in GitHub (repo Settings → Secrets and variables → Actions) update SUPABASE_ACCESS_TOKEN with it, then say "rerun" — I re-run e2e, merge, watch main, confirm Vercel. Also: retire jahjah-dispatcher (last job 2026-09-04)? yes/no
=== END ===
LESSON: a sabotage run on the live lane found two real bugs a review had missed (a `[ … ] &&` ending a group skipped its `mv`; REST spells issue state in lowercase) — exercise root automation on throwaway issues before trusting it.
