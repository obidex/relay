KIND: final
`docs/DECISIONS.md` D199–D249 now has the shape the caps prescribe — rule, trigger, status, pointer —
every entry at most 15 lines, the whole region down from 1653 lines to 385. Nothing was thrown away:
every mechanism, measurement and war story moved into a file that is NOT served publicly (the module
pitfalls files, and the backup and testing runbooks), so the register points and the detail has one
home. D001–D198 are byte-identical, proven by hashing the prefix before and after. D233 is marked
superseded by D239 and D245's Rule 3 by D246, as the card asked. Two open defects the old register
described on the public page by their vehicle now carry only their shape and their number. A
fresh-context reviewer read the diff three times and raised seven findings — all of one kind, "this
entry points at a file that does not carry what it promises" — and all were fixed and re-verified.
CI green, merged, `main` green, deployed, and the public mirror is serving the new register.

=== RELAY ===
HEAD: 07734cd | tree: clean
CI: pass — post-merge `main` run https://github.com/obidex/jahjah-internal/actions/runs/34663080297 (all jobs green, `ci-ok` success); Vercel deploy success
DONE: D199–D249 rewritten to rule · trigger · status · pointer, every entry ≤ 15 lines (51 entries, longest exactly 15) · D001–D198 byte-identical (sha256 `0eedf29c…75b1` before and after) · D233 marked SUPERSEDED by D239, D245 Rule 3 marked superseded by D246 · mechanisms MOVED to off-mirror homes in 8 pitfalls files and 2 runbooks · identifier proof in the PR body: 574 → 404 distinct backticked identifiers, 183 left the region, 174 still resolve elsewhere in `docs/`, 9 justified (8 `stale-fact`, 1 `dead-reference`) · canon-lint clean, every cited D-number and path still resolves · PR #160 squash-merged, branch deleted
FILES: 11 — `docs/DECISIONS.md` (+ 8 × `docs/pitfalls/`: catalog, docs-mirror, imports, infra-vps, inventory-stock, money-fx, permissions, sales; + `docs/runbooks/backup.md`, `docs/runbooks/testing.md`); +480 / −1562
FINDINGS/BLOCKERS: none open. Seven review findings, all fixed and re-verified clean: D203, D204, D206, D211, D215, D222 pointed at homes that did not yet carry their mechanism (each mechanism landed, each pointer re-aimed); D242 had genericised two product names in a dated historical entry (restored). Worth the strategist knowing: the pre-rewrite register named an OPEN defect's RPC and parameter, and another's two movement types, directly on the mirrored page — both now shape-and-number only, mechanism off-mirror (`D235`). Nine backticked identifiers left the whole `docs/` tree, every one of them a superseded proposal, a moved file:line, or a version measured on a date.
NEXT-NEEDED: none
=== END ===
LESSON: A compaction pass is finished only when every pointer it writes has been opened and read — "the mechanism is in X" is a claim, not a move, and all seven findings across three review rounds were pointers promising a home nothing had been moved into yet.
