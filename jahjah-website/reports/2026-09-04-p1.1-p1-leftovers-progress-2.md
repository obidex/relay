**Two of the five tasks are done and both are merged and live.** PR #34 — the accessibility PR that
had been held open since P1 waiting for a native Arabic reviewer — is on `master`, released by the
owner's amendment to W025 rather than by waiving it: the strategist's approval ships the string and
the native pass becomes a batched review afterwards. Every Arabic page's accessible labels are now
Arabic. And the four minor/patch Dependabot updates are applied in one chunk PR with secrets present,
which is the flow W114 settled; the bot's four PRs are closed naming ours. **Two things went
imperfectly and both are recorded rather than smoothed over**: the squash of #34 kept the *original
commit's* message, so `master` permanently carries a subject and body saying the PR must not be
merged — my miss, unfixable now — and the dependency run silently stripped 29 lockfile fields that no
update asked for, which the reviewer caught and which is repaired.

=== REPORT: P1.1-p1-leftovers · progress ===
HEAD: fa6df63 | tree: clean | branch: master (T2 done, T3 next) | git ls-files: 77

PRs:
  #34 0488dc1 MERGED — T1: the last two Arabic accessible labels. Brought up to date with master
      by a MERGE commit, not a rebase (a rebase of a pushed branch needs a force-push, which
      CLAUDE.md forbids; the squash makes the master end state identical). `npm run reference`
      re-run afterwards, no drift.
  #37 fa6df63 MERGED — T2: the four minor/patch Dependabot updates, lockfile only.

CI:
  #34  PR run 33919535315 green (1m29s) · post-merge master run 33920937648 SUCCESS at 0488dc19
  #37  PR run 33922369411 green (1m37s) · post-merge master run 33923325937 SUCCESS at fa6df633

PROD:
  dpl_AtqDyjf5h2vttmLAAk65evu4aCrw  READY  target=production  sha 0488dc19  (T1)
  dpl_BLGQL3HXrCPHtvXu9pW5bi1ck5Vt  READY  target=production  sha fa6df633  (T2)

Live probes (after the T2 merge):
  /                200      /ar/             200      /products/       200
  /sitemap-0.xml   200      /robots.txt      200      /admin           200

DONE:
  T1 — PR #34 merged. Acceptance MET, verified on the live site rather than only in dist/:
       `grep -o 'aria-label="[^"]*"'` over the live Arabic product page returns 11 labels, and the
       only one containing any Latin is `aria-label="Jahjah Trading Company — الرئيسية"` — the
       company name, which W022 keeps Latin. `مسار التنقل` (breadcrumb) and `عرض الصورة 1/2`
       (gallery thumbs) are serving. Production READY at the merge sha.
  T2 — PR #37 merged. Acceptance MET. undici 7.29.1 (+ 6.28.1 in the separate 6.x tree),
       browserslist 4.28.9, dompurify 3.4.14, @astrojs/sitemap 3.7.4, @sanity/astro 3.5.1,
       @sanity/client 7.27.0, react and react-dom 19.2.8. package.json byte-identical to master.
       `sanity` and `@sanity/vision` unmoved at 5.24.0; `astro` unmoved at 6.2.2.

DEPENDABOT DISPOSITION:
  #14 undici (security)        CLOSED — "applied in #37 (P1.1)"
  #15 browserslist             CLOSED — "applied in #37 (P1.1)"
  #16 dompurify (security)     CLOSED — "applied in #37 (P1.1)"
  #19 grouped minor/patch      CLOSED — "applied in #37 (P1.1)"
  #20 sanity 5→6               OPEN, untouched — F34's named chunk
  #21 @sanity/vision 5→6       OPEN, untouched — F34's named chunk
  NEW, opened by the bot within a minute of the #37 merge as it rescanned the new lockfile:
  #38 brace-expansion · #39 tar · #40 postcss — all INDIRECT. Not in this chunk's scope (the plan
  names four PRs by number). They are the next chunk's, under the same W114 flow.

  npm audit over the same tree: 30 vulnerabilities before (1 critical / 17 high / 10 moderate /
  2 low) → 24 after (1 / 15 / 6 / 2). The remaining critical and most of the highs are inside the
  Studio 5.x tree and cannot be reached by a range bump — they need F34's majors.

AR STRINGS SHIPPED (strategist-approved per the W025 amendment; for the batched native review):
  nav.breadcrumbLabel   = مسار التنقل        (breadcrumb landmark, read aloud only)
  products.viewImage    = عرض الصورة {n}     (gallery thumb, read aloud only)
  products.variantLabel = الخيار {n}         (variant swatch — ALSO a visible tooltip; renders on
                                              zero pages today, since every variant has a colour
                                              name, so it is untested against real output)
  PR #34's body keeps five specific questions for that review — the breadcrumb term, the `عرض`
  homograph, definiteness and gender in the ordinal, Western vs Arabic-Indic digits, and whether
  `الخيار` reads sensibly inside a radiogroup already named `اللون`.

CODEX (all three surfaces read, per W113/W122):
  #34 — `gh pr view --json reviews` 0 · `gh api …/pulls/34/comments` 0 · `gh api …/issues/34/comments`
        0 from `chatgpt-codex-connector`. SILENT for 7h33m from opening, and still silent 12 minutes
        after an explicit `@codex review` comment. Merged on the executor reviewer plus green `ci`.
        SILENCE IS NOT APPROVAL and is not recorded as such.
  #37 — same three surfaces, all 0. SILENT for 13 minutes from opening. Same disposition.
  That is 0 of 2 this chunk, against 4 of 10 across P1.

EXECUTOR REVIEWER (the gate, and it earned its place twice):
  T1 pass — clean on THE BAR; one FIX and seven NOTEs. The FIX was the PR title and body (below).
       It independently confirmed EN/AR key symmetry at 119/119, that `buildThumbRow`'s scope
       attribute reaches both the button and its `img`, that the `'{n}'` fallback is unreachable,
       and that zero `innerHTML` remains under `dist/products/`.
  T2 pass — one FIX, four NOTEs. The FIX is the lockfile repair below; it reproduced the cause in a
       scratch directory rather than inferring it, and it independently verified that all 1241
       `resolved` URLs are on registry.npmjs.org, all 1241 `integrity` values are `sha512-`, and
       `hasInstallScript` is on exactly the same three packages as master.

FINDINGS:

  1. THE #34 SQUASH KEPT THE ORIGINAL COMMIT'S MESSAGE, NOT THE PR TITLE — and `master` cannot be
     rewritten. The plan said to remove "[AR: pending native review]" from the merge commit subject.
     I edited the PR title before merging and confirmed it, then ran
     `gh pr merge --squash --delete-branch 34` without `--subject`. GitHub took the branch's own
     first commit message instead, so commit 0488dc1 permanently reads
     "...accessible labels [AR: pending native review] (#34)" with a body opening "THIS PR IS NOT
     PRE-AUTHORIZED AND MUST NOT BE MERGED." Both statements are false as of the owner's ruling and
     both are now in the permanent record, including in the Vercel deployment metadata.
     THE RULE THAT FOLLOWS, and it is already applied: pass `--subject` (and `--body` where it
     matters) explicitly whenever the merge commit message matters. #37 was merged with
     `--subject` and its message is correct. Going into DECISIONS as part of T5.

  2. THE DEPENDENCY RUN STRIPPED 29 `libc` FIELDS THAT NO UPDATE ASKED FOR — found by the reviewer,
     repaired before the PR opened. `@img/sharp-*` and `@rollup/rollup-linux-*` entries lost their
     `libc` discriminator although none of them changed version. Reproduced against master's
     untouched lockfile in a scratch directory: this box's npm (10.9.8) simply does not emit the
     field, and the lockfile was authored by one that does — it has been present at every commit
     since the repo's first. Without it npm cannot tell `…linux-x64-gnu` from `…linux-x64-musl`
     among 29 entries whose `os`/`cpu` pairs are identical. CI and Vercel are both glibc so nothing
     was going to go red, but a musl install could pick the wrong binary and the next `npm install`
     under an npm 11 would re-add all 29 as churn. All 29 restored in npm's own key position, 0
     skipped; count now matches master, `npm ci` accepts it, diff shrank by exactly 87 lines.
     NOTE FOR THE BOX: the executor's npm is older than the one that wrote this lockfile. Any
     future dependency work here will strip the field again unless it is checked for.

  3. A `@sanity/client@8.5.0` IS NOW IN THE TREE AND CANNOT REACH THE DATA LAYER. Given F34 reserves
     the Sanity majors, this needed proving rather than asserting: `require.resolve('@sanity/client')`
     from `src/utils/sanity.js` returns **7.27.0**. The 8.x copy exists only at
     `@sanity/astro > @sanity/visual-editing > @sanity/types > @sanity/client`, and that subpath is
     never imported — `astro.config.mjs` enables no visual-editing option and no visual-editing code
     reaches `dist/`. It becomes a real question the moment anyone turns visual editing on.

  4. THREE MAJOR BOUNDARIES ARE CROSSED INSIDE THE AUTHORIZED CLOSURE, and "no majors" was this
     task's headline, so they are named rather than left in a diff: top-level `uuid` 8.3.2 → 11.1.1
     (via `@sanity/uuid` 3.0.3 moving its own range, after which npm deduped the nested copies),
     `@sanity/visual-editing-types` 1.1.8 → 2.1.1, and `@sanity/comlink`'s nested `uuid` 13.0.2 →
     14.0.2. All npm-legal, each traced to a named package, `npm ls --all` reports no `invalid`.

  5. THE EMBEDDED STUDIO'S RENDER STACK MOVED ALTHOUGH `sanity` DID NOT — `@sanity/ui` 3.2.0 → 3.5.4,
     admitted by `sanity@5.24.0`'s own `^3.1.14`, newly nesting `@sanity/icons@5.2.1` and
     `motion@13.2.0`. Inside declared semver and transitive of an authorized package, so not a scope
     breach — but `/admin` is the one surface nothing in CI exercises beyond asserting it is
     `noindex`. STATICALLY CHECKED: the 5.2 MB Studio bundle builds, is referenced from
     `dist/admin/index.html`, and carries the product schema field names; `/admin` returns 200 in
     production. NOT CHECKED, because it is not machine-verifiable from here: that the Studio
     actually renders — the preview URL is behind Vercel deployment protection (302 to login), and
     confirming the slug field and the colour list still draw needs a person opening `/admin`.
     Recorded as a follow-up, not claimed as verified.

  6. Reported in the T0 report and repeated here because it is the chunk's most portable finding:
     both dispatched runs of this project died on the Claude subscription's 5-hour window, leaving
     53-byte logs and no report. The proposed `run-chunk.sh` / `web-dispatch.sh` change is in that
     report, for the ERP side.

DEVIATIONS:
  · T1 said "rebase on master"; a merge commit was used instead. A rebase of a pushed branch needs
    a force-push, which CLAUDE.md's "Never" list forbids, and the squash makes the end state on
    master byte-identical either way. The reviewer reached the same conclusion independently.
  · T2 said to commit the `npm run reference` dependency-table diff. There is none, and that is the
    correct outcome rather than a skipped step: the reference records `package.json` *ranges*, and a
    lockfile refresh does not touch them. This is F23's mechanism seen from the other side — it is
    precisely because Dependabot DOES rewrite those ranges that its own PRs drift the reference red.
  · Several packages landed newer than the version Dependabot named (@astrojs/sitemap 3.7.4 vs
    3.7.3, @sanity/client 7.27.0 vs 7.26.2, browserslist 4.28.9 vs 4.28.8, undici 7.29.1 vs 7.29.0).
    `npm update` takes each to the top of its existing caret range and the bot's PRs are days old;
    every one is still a patch or minor step the range already permitted.

CANON: none yet — T5.

NEXT-NEEDED: none. Continuing to T3 (the F40 hidden-products guard).
=== END ===
