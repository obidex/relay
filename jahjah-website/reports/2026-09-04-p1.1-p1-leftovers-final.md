**P1.1 is done and P1 closes with it. Five pull requests, all merged, all live.** The accessibility fix
that had been held open waiting for an Arabic reviewer is shipped — released by your own amendment
rather than by waiving anything. The four security and minor dependency updates are applied and the
bot's pull requests closed. The hidden-product guard can no longer report success without actually
having checked, which closes two findings the reviewer made three days ago that nobody had ever read.
And your launch-fact ruling is applied, so the site has stopped saying two different things about
itself: "authorized dealer" is gone from the home page and from the structured data Google reads.

**Three things need saying plainly, and none of them is a success.** First, this chunk ran
interactively because the dispatched executor died — the second time out of two — and the cause is not
in this repository at all; it is the Claude subscription's five-hour window, and the fix belongs to the
dispatcher on the ERP side. Second, one merge left a permanently wrong commit message on `master`,
saying the pull request must not be merged. That was my mistake and it cannot be repaired. Third, and
worst: **this chunk published a false claim about the code reviewer, twice, before catching it.** I
reported that Codex had gone silent on three pull requests. It had answered every one of them within
three minutes — by adding a 👍 reaction, which is a place I was not looking, and which its own
documentation had told me about in text I had already read. The correction is in canon as W130, and
the number that matters is that Codex has answered **9 of 10** pull requests, not the 4 the project
believed.

**The next chunk should fix one thing first**: four files still tell reviewers to block on unreviewed
Arabic, which is the rule you replaced. Two of them are gates that actually fire.

=== REPORT: P1.1-p1-leftovers · done ===
HEAD: 03d6cd9 | tree: clean | branch: master | git ls-files: 77
Issue: #36 — closed by this report (W127)

PRs:
  #34 0488dc1 MERGED — T1: the last two English aria-labels reaching Arabic pages, localized.
  #37 fa6df63 MERGED — T2: the four minor/patch Dependabot updates, lockfile only.
  #41 c336235 MERGED — T3: the hidden-product guard cannot pass without checking (F40, F37).
  #42 a85edc1 MERGED — T4: the W088 launch-fact ruling applied.
  #43 03d6cd9 MERGED — T5: cleanup and canon close.
  #27 CLOSED unmerged — stale duplicate of #28; branch chunk/p1-t3 deleted.

CI: green on every PR and on every post-merge run on master.
  PR runs    #34 33919535315 · #37 33922369411 · #41 33926894210 · #42 33928518782 · #43 33930218151
  master     33920937648 · 33923325937 · 33926997198 · (T4) · 33930318114

PROD:
  dpl_AtqDyjf5h2vttmLAAk65evu4aCrw READY production 0488dc19
  dpl_BLGQL3HXrCPHtvXu9pW5bi1ck5Vt READY production fa6df633
  dpl_FYA4nJScgVbtYcF6He5Hetv2arpr READY production 03d6cd9f  ← current

Live probes, after the final merge:
  /              200      /ar/           200      /products/     200
  /sitemap-0.xml 200      /robots.txt    200      /admin         200
  Home feature card, live: EN "Manufacturer, main supplier and distributor" ·
  AR "مصنِّع ومورِّد رئيسي وموزِّع" — the ruling is serving.

DONE:
  T0 — Established why both dispatched runs died. NOT this repository. Issue relabelled.
  T1 — #34 merged. No Latin-only accessible label reaches an Arabic page except the company name,
       verified on the live site, not only in dist/.
  T2 — #37 merged. npm audit 30 → 24 vulnerabilities; four bot PRs closed naming it.
  T3 — #41 merged. Both of Codex's P1s on #28 fixed and answered in-thread.
  T4 — #42 merged. No unmade agency claim anywhere in the built site; founding year already 2010.
  T5 — #43 merged. #27 closed, canon updated, W123–W130 appended.

DEPENDABOT DISPOSITION:
  #14 undici (security)     CLOSED "applied in #37 (P1.1)"     #19 grouped         CLOSED, same
  #15 browserslist          CLOSED, same                       #20 sanity 5→6      OPEN, untouched (F34)
  #16 dompurify (security)  CLOSED, same                       #21 vision 5→6      OPEN, untouched (F34)
  NEW: #38 brace-expansion · #39 tar · #40 postcss — opened by the bot 72–84 s after #37 merged, as
  it rescanned the refreshed lockfile. All indirect. Not in this chunk's named scope; next chunk's.

AR STRINGS SHIPPED (strategist-approved per W125; batched for native review — ROADMAP F43):
  nav.breadcrumbLabel   = مسار التنقل            (#34, read aloud only)
  products.viewImage    = عرض الصورة {n}         (#34, read aloud only)
  products.variantLabel = الخيار {n}             (#34, ALSO a visible tooltip; renders on 0 pages today)
  home.featureDealer    = مصنِّع ومورِّد رئيسي وموزِّع  (#42, VISIBLE BODY COPY on both home pages)
  PR #34 carries five specific questions for the reviewer; #42 carries the diacritics question. The
  diacritics on the last string are deliberate — three Form II participles, each ambiguous unvowelled
  — and were themselves corrected by the executor's reviewer from an inconsistent first draft.

CODEX — READ ALL FOUR SURFACES. THIS CHUNK GOT THIS WRONG AND PUBLISHED IT.
  #34  answered 2m07s after an explicit @codex review   issue comment + 👍   no findings
  #37  answered 2m41s unprompted                        👍                   no findings
  #41  answered 3m26s after an explicit @codex review   issue comment + 👍   no findings
  #42  answered 1m52s unprompted                        review + inline      ONE P1
  #43  answered ~2m unprompted                          👍                   no findings

  **Codex answered 5 of 5. My earlier reports in this thread said it was silent on #34, #37 and #41
  "across all three surfaces". That was false**, and it went to the issue and to the world-readable
  relay before I caught it. The cause: there is a FOURTH surface — the 👍 REACTION
  (`gh api …/issues/<n>/reactions`) — which appears on none of the comment surfaces and is Codex's
  "reviewed, found nothing" verdict. **Its own boilerplate states this**, in text this session had
  already read and quoted: *"If Codex has suggestions, it will comment; otherwise it will react
  with 👍."*

  RE-MEASURED AGAINST P1, because canon carried the same error: of the six PRs recorded as silent,
  FIVE carry a 👍 (#25, #29, #30, #32, #33). **Codex answered 9 of 10, not 4.** The two signals are
  complementary, which corroborates the rule: a PR with findings has no 👍, a PR without has one, and
  the only PR with both is #31, whose explicit re-review found nothing.

  The one P1 it raised — on #42, against the Arabic string — was CORRECT against `AGENTS.md` as
  written, and is answered in-thread. See FINDINGS 1.

EXECUTOR REVIEWER — six passes, five changed the shipped result:
  T1  one must-fix (the PR title and body, which the squash then ignored anyway — see FINDINGS 2).
  T2  found the lockfile had silently lost 29 `libc` fields no update had asked for. Reproduced it.
  T3  found my first fix closed ONE of THREE doors to the same vacuous pass.
  T3b re-review: no fixes; caught the header over-claiming what it had closed.
  T4  found the ruling applied only two of its three elements, and two overstated comments.
  T5  the fact-check that caught the Codex error above, plus ten other false or overstated claims.

FINDINGS:

  1. FOUR FILES STILL CARRY THE ARABIC GATE YOU REPLACED, AND TWO ARE GATES THAT FIRE (ROADMAP F42,
     high). `AGENTS.md` item 4 is what Codex reads — it produced a correct P1 against a rule you had
     amended hours earlier, and it will do so on every future Arabic string. `.claude/agents/
     reviewer.md` lists an unmarked Arabic string as a BLOCKING finding, so the executor's own gate
     would now block strings W125 says ship. `REVIEW.md` carries the old BAR verbatim. Softer residue
     in `.claude/skills/ship/SKILL.md`. None was in this chunk's authorized file list. **`AGENTS.md`
     changes how an external system behaves, so the wording is the strategist's to settle.**

  2. #34's MERGE COMMIT ON `master` IS PERMANENTLY WRONG, AND IT IS MY MISTAKE. The plan said to
     remove "[AR: pending native review]" from the merge subject. I edited the PR title, confirmed
     it, then merged without `--subject` — and GitHub took the branch's own first commit message
     instead, subject AND body. Commit 0488dc1 permanently reads "…[AR: pending native review] (#34)"
     over a body opening "THIS PR IS NOT PRE-AUTHORIZED AND MUST NOT BE MERGED", both false as of
     your ruling, and both copied into the Vercel deployment record. `master` cannot be rewritten.
     #37, #41, #42 and #43 were merged with `--subject` and are correct. W129, and the rule is now in
     CLAUDE.md §9.

  3. `gh issue close` IS NOT ALLOWLISTED, AND W127 NOW REQUIRES IT (ROADMAP F46, high). The skill
     closes the chunk issue on `final`. An interactive session is prompted once; a DISPATCHED session
     is simply refused, and cannot repair the gap because it may not edit `.claude/**` at all. One
     line in `.claude/settings.json`, which this chunk was not authorized to touch.

  4. THE HIDDEN-PRODUCT GUARD'S WITNESS RESTS ON ONE PRODUCT (ROADMAP F44). Of 22 visible products,
     only `dcel-washer-front-7kg` emits Sanity image URLs, and CI passes the guard no
     `PUBLIC_SANITY_*`. Unpublish that product and `ci` goes RED from a content edit with no code
     change. Correct on the guard's own terms, invisible to whoever made the edit. The fix is a
     Tier-3 `.github/**` change this chunk could not make.

  5. TWO MORE SILENT-SKIPS OF THE SAME FAMILY (ROADMAP F45). `verify.sh`'s secret-in-`dist/` leak
     check reads shell variables and `verify.sh` never loads `.env.local` — so on the executor all
     three leak checks print nothing and do not run. That is the leak check itself silently skipping.
     And the Organization JSON-LD now emits a variable through `set:html`; `finalDescription` is
     Sanity-derived on 44 product routes, and only the `isHome` gate keeps CMS text out of that raw
     sink. Neither is exploitable today.

  6. THE DISPATCHED LANE — FOR THE ERP SIDE, unchanged from the T0 report. Both runs carrying a real
     chunk died on the subscription window: #24 after 5307 s, #36 after 523 s, each leaving a 53-byte
     log and no report. Proposed `run-chunk.sh` changes: capture the transcript with
     `--output-format stream-json --verbose`; treat a usage-limit exit as RETRYABLE — restore
     `chunk:approved`, do not book a failure, refund the daily-cap slot, honour the reset time.
     Secondary: the three-strike self-disable cannot fire for a chunk failure, because the poll that
     books it goes on to call `jj_finish_ok` one second later. Measured in the log.

DEVIATIONS:
  · T1 said "rebase on master"; a merge commit was used, because a rebase of a pushed branch needs a
    force-push, which CLAUDE.md forbids. The squash makes master identical either way.
  · T2's expected reference diff does not exist: the reference records package.json RANGES, and a
    lockfile refresh does not move them. Correct outcome, not a skipped step.
  · T4's acceptance `grep -rho '2009' dist/ | wc -l` = 0 is unsatisfiable — it returns 3, all inside
    the Sanity Studio bundle (two SVG path coordinates and a thin space, traced to @sanity/icons and
    lodash). Scoped to `*.html` it is 0, which is what the check was reaching for.
  · Three "no work needed" results in T4, each investigated: the founding year was already 2010
    everywhere; SUNNY/DSP already read "exclusive agent … in Syria" and `الوكيل الحصري` and live in
    Sanity, which this chunk may not write; site.js, HomeContent.astro and about.astro needed nothing.
  · `echo $CHUNK_ISSUE` printed 24, a stale inherited value. The prompt's first line said 36 and was
    used throughout — exactly the case W099 anticipated.

CANON: docs/STATE.md · docs/ROADMAP.md · docs/DECISIONS.md (W123–W130) · docs/STRATEGIST.md ·
       CLAUDE.md · .claude/skills/relay-report/SKILL.md · docs/reference/site.md (generated)

NEXT-NEEDED: nothing blocking. **P2 · identity and foundation** is next, and its single first
decision is yours: **Vercel Pro (~$20/mo)**, which W090 requires before the first on-demand route
ships — that is P2's second item, so it is needed early in P2 rather than at launch.

HANDOVER FOR THE NEXT STRATEGIST (the chat rotates after this chunk):
  · START AT `docs/STATE.md`, then ROADMAP's register. P1 and P1.1 are closed; nothing is owed.
  · P2 IS NEXT and is Tier 3 throughout: SKU under GATE 1 (W076) · the Vercel adapter and the first
    on-demand route · Supabase project #2 with RLS and MFA · a TypeScript data layer in src/lib/.
  · THE ONE DECISION TO PUT TO THE OWNER: Vercel Pro, before P2's second item. Nothing else blocks.
  · FIX F42 FIRST — it is cheap and it makes the owner's own Arabic amendment effective. F46 next.
  · DO NOT DISPATCH P2 UNLESS THE ERP SIDE HAS FIXED THE LANE (W128). The dispatched executor has
    never carried a real chunk to completion. Approve it when someone can run it interactively.
  · READ ALL FOUR CODEX SURFACES, including reactions. This chunk got that wrong and published it;
    W130 is the entry, and the corrected record is that Codex answers almost everything, fast.
  · OWNER ITEMS STILL OPEN, all low: the three tier names, the currency (W066), the showroom address,
    a confirmation pass on numbers/hours/warranty, and which of the 22 placeholder products to keep —
    which is now purely his, since W126 makes hiding his own act through the `published` toggle.
=== END ===
