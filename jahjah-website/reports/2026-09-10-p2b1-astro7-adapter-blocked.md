**In plain words:** Astro 7 builds the site correctly: 68 pages, every check passes, the five dependency updates are in, and `npm audit`'s critical is gone. But the move changes one thing visitors can see. Astro 7's new CSS minifier rewrites every mobile-layout breakpoint into a newer syntax that iPhones before iOS 16.4 (and a few very old browsers) do not understand, so on those phones the site shows its desktop layout. A one-line setting keeps the old behaviour; I built with it and it restores all 23 breakpoints. The plan does not authorize that line, and the executor's reviewer ruled the PR cannot self-merge while this is undecided. **So PR #62 is open and green-pending, not merged, and the chunk is paused on one decision: A or B below.**

=== REPORT: P2b-1-astro7-adapter · blocked ===
HEAD: master 8d037e5 (unchanged) | tree: clean | branch: chunk/p2b1-t1-astro7 at 54039a0
PRs: #62 54039a0 open — T1 Astro 7 + Dependabot #47 #48 #49 #50 #55 (not merged: reviewer FIX, decision below)
CI: #62 `ci` green — run 34463412434 (1m6s, tier3-guard passed) · https://github.com/obidex/jahjah-website/actions/runs/34463412434
PROD: deployment 6367834996 (`8d037e5`, #60) READY/success — untouched by this chunk | live probes 7/7 200 (below; master unchanged, so this is the pre-T1 baseline)
DONE:
- Preflight: build 68 pages · verify 0 FAIL 0 WARN (67) · 79 tracked files · `gh` auth ok · env names present · pristine `.astro/dist-master/` kept. `npm audit` baseline 22 (1 critical / 13 high / 6 moderate / 2 low).
- T0: labels were already moved when this session started — `chunk:running` added 09:28:01Z, `chunk:proposed` removed 09:28:35Z (issue timeline); confirmed, not repeated. `chunk:approved` never applied.
- T1: peer ranges checked first (`@sanity/astro` 3.5.1 declares `^7.0.0`; `@astrojs/sitemap` 3.7.4 declares no astro range at all — see DEVIATIONS) → `npx @astrojs/upgrade` (astro 6.2.2 → **7.3.2**, sitemap range → ^3.7.4) → `npm update svgo linkify-it` (**4.1.0**, **5.0.2**) → sharp **0.35.4** arrived with astro 7.3.2's own `optionalDependencies.sharp ^0.35.4`. `sanity`/`@sanity/vision` stayed at 5.24.0 (F34). 29 `libc` fields restored (W123; exactly 87 added lines against npm's output). `compressHTML: true` added. #55 pinned at `d75b94d5…` `# v1.0.216` (the annotated tag, dereferenced through the API). No template was rejected by the Rust compiler. Build 68 pages · verify 0 FAIL 0 WARN · build `[WARN]` lines 130 → 1 · `npm audit` 22 → **17** (critical 1 → **0**, high 13 → 9).
- T1 acceptance, `dist/` against master's build: all 68 HTML files differ byte-wise, and **every difference is classified by name, with zero left over.** The classes are scope-hash renames (9108 occurrences both sides), asset hashes, whitespace, entity spelling (`&#38;`→`&amp;`, `&#34;`→`&quot;`), inline module scripts re-minified by Rolldown/Oxc (same count, same literals), and inline CSS re-minified by Lightning CSS. Byte-identical: sitemap, robots.txt, and all 82 JSON-LD blocks. **CSS rule count unchanged** (73/73, 51/51, inline 746/746). `/admin` builds and references the Studio bundle; its inline style lost one CSS marker (`--static-css-file-loaded-vision`) that no JS anywhere in `node_modules` reads.
- T2–T5: not started (T2 builds on T1).
DEVIATIONS:
- T1 not self-merged, although GATE 2 pre-authorizes it: the executor's reviewer returned a FIX (below), so GATE 2's "clean executor reviewer" is not met.
- The plan's T1 check "`@astrojs/sitemap` must declare support for astro 7" was **interpreted**, not met literally: it declares no astro peer at all, so nothing excludes astro 7. It imports only from `'astro'`, uses three hooks, and its output is byte-identical.
- `astro.config.mjs` carries `compressHTML: true` **and one comment line** explaining it. That is beyond the letter of "ONLY `compressHTML: true`", and is named in the PR.
- The preflight's "tree clean" was false at session start: a previous session of this chunk had left uncommitted T1 edits on `chunk/p2b1-t1-astro7`, having posted no report. They were stashed by name (`stash@{0}`: "debris from interrupted session of chunk #61 P2b-1 T1 …"), not applied, and T1 was redone from master (W058). Left in place, as with the older stashes.
FINDINGS/BLOCKERS:
- **BLOCKER — Astro 7 lowers the CSS browser floor and drops the mobile layout on older iPhones.** Astro 7 builds with Vite 8.2.2, which minifies client CSS with **Lightning CSS** at its default `cssTarget` `['chrome111','edge111','firefox114','safari16.4','ios16.4']`. Astro 6's Vite 7.3.3 used esbuild at `['chrome107','edge107','firefox104','safari16']`. Lightning CSS therefore rewrites **all 23 breakpoints** (640/768/960 px; the source writes `max-width`) into media-range syntax: `@media (max-width:640px)` → `@media (width<=640px)`. Safari/iOS before 16.4, Chrome before 104 and Firefox before 63 ignore those rules completely, so for example an iPhone that stops at iOS 15 gets the desktop layout. The executor's reviewer: "passes the plan's letter (rule counts unchanged) but not its intent — compressHTML: true was pinned expressly 'to keep today's output'… Do not self-merge T1." Not in THE BAR, and not in the plan's STOP list. It waits because the reviewer gate is not clean and the fix is an unauthorized Tier-3 line.
- **Option B is measured.** A scratch config (gitignored `.astro/`, nothing tracked touched) that adds only `vite: { build: { cssTarget: ['chrome107', 'edge107', 'firefox104', 'safari16'] } }` builds 68 pages with **0 range-syntax queries and 23/23 `max-width`**.
- **20 new linux gnu/musl lockfile entries arrive without `libc`** (`@astrojs/compiler-binding-*`, `@bruits/satteri-*`, `@rolldown/binding-*`, `lightningcss-*`), although the registry says they carry it. The plan names "the 29", so these were left alone. The build is unaffected (the loaders detect libc at runtime); it is W123's gap, 20 entries wider. For W141.
- `npm audit` on master's tree had drifted from STATE's 21/0-critical to 22 with 1 critical since P1.2. T1 clears it.
- `/admin`'s Vision tool is **not runtime-verified** — that needs a browser; its JS chunk is built.
Dependabot: #47 #48 #49 #50 #55 all open, applied in #62, to be closed "applied in #62 (P2b-1)" after it merges · #20 #21 untouched (F34).
Codex (#62, from `createdAt` 09:56:38Z): 👀 at open (not a verdict, and since removed) · **review 09:59:39Z (3m01s) carrying ONE inline P2 on `package.json:22`: the same breakpoint regression, found independently**, with the same remedy ("pin `vite.build.cssTarget` to a floor that preserves legacy `max-width` syntax") · no 👍, consistent with findings being present · 0 issue comments. Four surfaces read: reviews 1 · inline 1 · issue comments 0 · reactions none now. Answered in the PR: agreed, measured, held for this decision. The executor's reviewer (FIX) and Codex (P2) are two reviewers from different model families reaching the same finding.
Live probes (production, master 8d037e5, before any merge): / 200 · /ar/ 200 · /products/ 200 · /admin 200 · /admin/structure 200 · /admin/structure/product 200 (all three /admin paths the 1117-byte Studio shell) · /sitemap-0.xml 200.
Preview (#62): deployment built (`Vercel` status success at 09:56:37Z), and **every path answers 302 → `vercel.com/sso-api`**: /, /ar/, /products/, /admin, /admin/structure, /admin/structure/product and /sitemap-0.xml all return 302 with a 15-byte body, both on the deployment URL and on the branch alias. T1's "`/admin` 200 on the preview" therefore **cannot be measured from this session**; it is measured in `dist/` instead.
- **FINDING — previews are behind Vercel Authentication, project-wide, and W090 assumes they are not.** Read-only Vercel MCP read of the project's protection: `ssoProtection.enabled: true, deploymentType: all_except_custom_domains`; password and trusted-IP protection off. #59's and #60's previews answer the same 302, so this PR did not cause it. The production alias `jahjah-website.vercel.app` stays public (200). **This breaks T2 as written:** the plan has the strategist fetch `/admin`, `/admin/structure` and `/admin/structure/product` on T2's preview, and an unauthenticated fetch gets the SSO redirect, never the Studio shell.
- **FINDING — a credential side effect, reported as §6 requires.** To try an authenticated read, I used the Vercel MCP's `web_fetch_vercel_url` on four preview paths. It still returned 302, and **each call minted a `_vercel_share` token** into the redirect URL it returned: four shareable-link tokens for this preview deployment, now in this session's transcript. They are not pasted here or anywhere. The preview carries only the public site plus the Studio shell, so the exposure is small. Still, by §6 they are burned: **the owner may revoke the deployment's shareable links in the Vercel dashboard** (owner-only). I will not use that tool again in this chunk.
CANON: none yet — STATE, ROADMAP, DECISIONS are T5.
NEXT-NEEDED: **A or B on PR #62.**
  **A — accept Vite 8's floor.** T1 merges as it stands; the site requires iOS/Safari 16.4+ (Chrome 104+, Firefox 63+) for its mobile layout. Recorded in W141.
  **B — authorize the one line (RECOMMENDED).** `vite: { build: { cssTarget: ['chrome107', 'edge107', 'firefox104', 'safari16'] } }` in `astro.config.mjs` (Tier 3), on PR #62, keeping Astro 6's floor. Measured: 23/23 breakpoints stay `max-width`. I re-run build + verify + the dist comparison + the reviewer, then merge on green `ci` and Codex. Recommended because the audience is Syrian mobile users, and older iPhones are plausibly over-represented there.
  Either reply resumes the chunk at T1's merge, then T2 as planned. Until then, nothing else is touched.
  **Not needed today, but before T2's pause — how does anyone verify a protected preview?** T2's verification fetches the preview, and every preview is behind Vercel Authentication (above). The options are all the owner's, because they are settings or credentials:
  (a) turn Vercel Authentication off for preview deployments in the dashboard. That makes W090's stated assumption true again, and every future preview becomes public.
  (b) **RECOMMENDED:** the owner, or the strategist through a logged-in browser, opens T2's preview at `/admin`, `/admin/structure` and `/admin/structure/product` and pastes what it serves. No setting changes and no credential is minted. The executor still posts every build-side measurement (`.vercel/output/config.json` routes, functions, byte comparison).
  (c) the owner creates a protection-bypass secret for automation. That is a credential, so it stays with the owner, and nothing may paste it.
  If none is chosen, T2 still opens and stops at its planned pause; only the preview half of its verification waits.
=== END ===
