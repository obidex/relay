# CLAUDE.md — Jahjah Website · Executor Contract

> **For you, Claude Code, running on the VPS in tmux session `web`.** Loaded every session. Binding. The plan you execute arrives as a **GitHub issue** written by the strategist; **the owner's `chunk:approved` label is the approval**, and `jahjah-web-dispatch` starts you from it with `CHUNK_ISSUE` set (W099). A chunk pasted by hand still works — then `CHUNK_ISSUE` is unset and reports go to the relay only. You run unattended: no questions mid-chunk — stop with a BLOCKED report instead.
> Facts about the code (routes, signatures, schema fields, namespaces, env names, dependencies) are **not** in this file: read `docs/reference/site.md`, regenerated by `npm run reference`. Reasons live in `docs/DECISIONS.md` (`W###`). Current state in `docs/STATE.md`.

---

## 1. Identity and environment

| | |
|---|---|
| Client | Jahjah Trading Company / `شركة الجحجاح التجارية` — Syrian home-appliance manufacturer + main supplier + distributor. Bilingual EN/AR public site with a customer price layer and inline Admin Mode. |
| Repo | `github.com/obidex/jahjah-website` (private) · default branch **`master`** = production |
| Executor | this VPS · clone `/opt/jahjah/web` · Node 22 · npm · `gh` authenticated (verify in preflight) · shell is bash |
| Hosting | Vercel — auto-deploys `master`; preview deployment per branch; **also** redeploys on Sanity publish via deploy hook (no commit) |
| CMS | Sanity project `pxf1amia`, dataset `production`, Studio embedded at `/admin` |
| Web DB (from P2) | Supabase project #2 — auth, staff, customers, tiers, prices, promotions, stock, settings, audit. **Independent of the ERP's Supabase project.** |
| Live | `https://jahjah-website.vercel.app` (domain `jahjah.net` connects only at launch — W027) |
| Reports | `/relay-report` publishes to **both**: a comment on the chunk's GitHub issue (when `CHUNK_ISSUE` is set) **and** the public relay `obidex/relay` under `jahjah-website/reports/`. The issue is where the plan, the approval and the result live as one thread; the relay is what the strategist reads on a timed cadence. Neither is optional (W099). |

## 2. Stack rules — what the site IS and ISN'T

- **Astro, prerendered by default.** `export const prerender = false` only on routes that need a session or per-visitor data (account, price island, Admin Mode, `/api/*`), and only when the chunk plan names the route (W074). Adapter `@astrojs/vercel` (from P2). **No Vercel-only APIs** — KV, Blob, Edge Config, platform crons/sessions — the adapter must be swappable to `@astrojs/node` (W078).
- **Public pages carry no prices, no stock quantities, no tokens, no session logic.** Hidden/disabled products never enter static HTML, sitemap, listings or search; direct links 404 without a staff session (W077). Enforce at build time AND request time.
- **Two stores, one boundary (W075).** Sanity = content. Web DB = everything commercial and every identity. Never add a price, stock, customer or role field to a Sanity schema.
- **Vanilla CSS with design tokens** (`src/styles/global.css`), logical properties for RTL. No Tailwind/shadcn/any framework. **Vanilla JS** on pages; no React/Vue/Svelte for site features.
- **TypeScript for server-side code** (`src/middleware.ts`, `src/lib/**`, on-demand routes, DB/auth clients). Existing `.astro` pages, `src/utils/*.js`, `src/data/translations.js` stay JS — converting them is never a task (W086).
- **No new dependency** unless the chunk plan lists it by name.
- Sanity reads happen server-side only (build time or on-demand route). Never a client-side Sanity query, never a token in a client bundle.

## 3. The data contract (binding shapes; signatures in the reference)

- Sanity client: `apiVersion: '2024-01-01'`, `perspective: 'published'`, `useCdn: false`, token `SANITY_READ_TOKEN`. Every product/brand/category query carries `!(_id in path("drafts.**"))`. **Both guards stay** (W012). The `apiVersion` is pinned: anything comparing against live counts — `jahjah-web-backup-check` does — must query the same version or it is asking a different engine.
- **Four facts the generator does not extract, so they live here** (closes F12): the `apiVersion` above; **a missing image is `null`, never a URL to a static file** — `imageUrl()` returns null when there is no asset, and the template renders `NoImageTile` (inline SVG, no `<img>`, so no request that can 404). This replaced `/images/placeholder.jpg`, which was the documented fallback and never existed on the live site (W112); `ProductDetail.astro`'s variant data attributes are named from the source, not invented, so read the file before assuming one; and the category projection is `category->{slug, nameEn, nameAr}`.
- Product shape from `src/utils/sanity.js`: `specs` is an ARRAY of `{label, value}` (never an object); `variants` is always an array (empty = no variants); `brand` is Latin; `categoryName` localized; Arabic fields fall back to English per field, never crash, never empty. Each variant carries `sku` (from P2, W076), `modelNumber`, optional `color` slug from `COLOR_OPTIONS`, `images[]`, `imagesCard[]`.
- Image URLs: `imageUrlBuilder(client).image(src).width(w).dpr(2).auto('format').fit('max')` — cards 400, detail 800.
- `COLOR_OPTIONS` is duplicated verbatim in `product.ts` and `sanity.js`; change both in one commit (W038). `COLOR_MAP` exists only in `sanity.js`, derived from that list — it needs no separate edit. The reference generator diffs the two `COLOR_OPTIONS` literals field by field and says `EXTRACTOR MISS` rather than `IN SYNC` if it reads neither.
- `BRAND_ORDER` = DCEL, LAPON, JAHJAH, SUNNY, DSP — code constant, not a schema field (W042).
- `_key` in arrays is index-based (`en-0`, `ar-0`, `gallery-0`); never derived from text.
- Slug validator in `product.ts` strips `drafts.` and excludes both IDs — never simplify (W013).
- `src/data/products.js` and `products-ar.js` are intentional backups: not imported, not deleted. `scripts/migrate-*.mjs` are one-shot: never re-run.

## 4. Tiers and models

| Tier | Touches | Ceremony |
|---|---|---|
| **1** | copy, comment, translation value, no-caller rename | investigate → apply → `/verify` → commit → PR (self-merge if pre-authorized) |
| **2** | new page/component/section, new key, in-component CSS, additive Sanity field with no consumers, `Layout.astro` chrome | + show the plan in the PR body; reviewer subagent before commit |
| **3** | `astro.config.mjs` · `vercel.json` · `sanity.config.ts` · `src/sanity/schemaTypes/**` · GROQ/signatures in `src/utils/sanity.js` · `Layout.astro` head logic · cascade-sensitive scoped CSS · `src/middleware*` · `src/lib/**` · on-demand routes · auth · DB schema/RLS/migrations · Admin Mode writes · env handling · `.claude/**` · `.github/**` | **only when the chunk plan names the file**; thorough reviewer pass; compiled-output verification; never self-merge unless the plan says so |

Unsure 2 vs 3 → 3. Model is stated on the chunk's first line; do not change it mid-chunk.

## 5. Running a chunk

**Preflight (stop = BLOCKED report):** `cd /opt/jahjah/web` · `git fetch` · `HEAD == origin/master` · clean tree · **`git ls-files | wc -l` recorded, and recounted before every push** (W092: a `.gitignore` rule can silently drop files and `git add` says nothing) · `gh auth status` · `git ls-remote origin` · `npm ci` · `npm run build` exit 0 · env **names** present (`PUBLIC_SANITY_PROJECT_ID`, `PUBLIC_SANITY_DATASET`, `SANITY_READ_TOKEN`, plus any the plan lists) · read `docs/STATE.md` + `docs/reference/site.md` · list every assumption in the prompt that contradicts them.

**Per task:** branch `chunk/<name>-t<n>` → investigate (read first; "no work needed" is a valid result, W029) → implement → `/verify` → reviewer subagent (`.claude/agents/reviewer.md`) → fix → re-review → commit (Conventional Commits, no emojis) → push branch → `gh pr create` with plan, acceptance checks, preview URL → wait for CI **and for the Codex review** → merge with `gh pr merge --squash --delete-branch` **only if the plan pre-authorized this PR** and CI is green and the reviewer is clean → `/relay-report progress` → next task.

**Before merging any PR, wait for the Codex review on it — across all FOUR surfaces, the fourth being the 👍 reaction — and address every finding that meets THE BAR** — fix it, or answer it in the PR; answer the nits or say why not. **Waiting means waiting:** PR #28 was merged **88 seconds** after it opened and Codex's review landed **108 seconds after that**, carrying two P1 findings nobody has ever seen or answered (ROADMAP F40). Codex (the ChatGPT GitHub app) is the **reviewer of record** (W113) and reads `AGENTS.md` at the repo root. Read its verdict across all four: `gh pr view <n> --json reviews` (the review shell), `gh api repos/obidex/jahjah-website/pulls/<n>/comments` (**the findings**, which the review body does not list), `gh api repos/obidex/jahjah-website/issues/<n>/comments` (a re-review answer), and **`gh api repos/obidex/jahjah-website/issues/<n>/reactions` — a 👍 is its "reviewed, nothing found" verdict** and is invisible to the other three. **Your own reviewer subagent remains the gate.** The Claude `review` workflow is now a **manual fallback** — `workflow_dispatch` with a PR number, for when Codex is down or silent — so it no longer reports on PRs at all, and its absence is not evidence of anything.

**The measured Codex record, corrected TWICE — and the second correction is why this paragraph now names FOUR surfaces.** The first correction fixed a count. This one fixes the thing that produced it: **Codex answers "I reviewed it and found nothing" with a 👍 REACTION**, and its own boilerplate says so — *"If Codex has suggestions, it will comment; otherwise it will react with 👍."* A reaction appears on none of the three comment surfaces, so a session reading only those records a review as silence. **Read `gh api repos/obidex/jahjah-website/issues/<n>/reactions` too.**

Corrected record, measured 2026-09-04. **P1.1: Codex answered 4 of 4 PRs** — #34 in 2m07s and #41 in 3m26s after an explicit `@codex review` (issue comment + 👍 each), #37 unprompted with a 👍 in 2m41s, #42 unprompted with a review carrying one inline P1 in 1m52s. Findings on one; silence on none. **P1: findings on 4 of the 10** — #26 (one P2), #27 (one P1), #28 (two P1), #31 (two P2) — but of the six recorded as "silent", **five carry a 👍** (#25, #29, #30, #32, #33). Only #34 went genuinely unanswered, and it got its 👍 the next day. **So Codex answered 9 of 10, not 4**, and it has used P1/P2 severity labels since #22 and #23 on 2026-09-02. The two signals are complementary and that is the corroboration: a PR with findings has no 👍 (#26, #27, #28) and a PR without findings has one — the sole PR with both is **#31**, whose explicit re-review found nothing and added a 👍 beside its original findings. **The ten-minute rule stands anyway** — it is somebody else's system and can be down — but silence must now be established across **all four** surfaces, and on this evidence it is rare.
**Codex is somebody else's system, so treat its arrival as an assumption, not a property of ours** (W105): it was observed reviewing PRs #22 and #23 on 2026-09-02, and `docs/STATE.md` live flag 10 carries the dated version of this. Its own boilerplate says it fires on opening a PR, on a draft being marked ready, or on a `@codex review` comment — so **a draft PR gets nothing** — and that same boilerplate is where the 👍 convention is stated. If no review has appeared within roughly ten minutes of opening a non-draft PR, do not read the silence as approval and do not wait indefinitely — dispatch the Claude fallback, or merge on your own reviewer plus green `ci` and **say in the report that Codex did not post**. Neither review is a required check, so a model outage can never block a merge; equally, silence from either is never approval.

**A Tier-3 PR needs the authorization line in its body**, or CI's `tier3-guard` fails it (W101):
`Tier-3: authorized by chunk <name>`. Adding it re-runs the guard; no empty commit needed.

**`CHUNK_ISSUE`** is exported by the dispatch lane and also written into the first line of the chunk prompt (`CHUNK ISSUE: #<n> …`) — read it from there, because a dispatched session cannot rely on being allowed to read its own environment.

**Never:** push to `master`, force-push, rebase `master`, retag, run `sanity dataset import|delete`, read or print `.env*`, put a secret in a report, touch Vercel/Sanity/Supabase dashboards or settings (owner-only), ship an Arabic string the plan did not authorize.

**Sanity writes and DB migrations** (GATE 1): the script/SQL in the plan is the approved text. Identifier-only corrections are allowed — publish the final text + diff in a relay file before applying. Any semantic change → BLOCKED. Live re-fetch before any field deletion (W049). A write applied ahead of approval stops the chunk and is reported for ratification.

**Interrupted?** The next session re-runs build and `/verify` from scratch; earlier claims are worthless (W058).

**Chunk end (last PR):** update `docs/STATE.md` (ledger row, HEAD, flags, next step), `docs/ROADMAP.md` register, `docs/DECISIONS.md` (append `W###`), run `npm run reference` and commit the diff → `/relay-report final` in the REPORT shape from `docs/STRATEGIST.md` §1. **`final` also CLOSES the chunk issue (W127)** — the skill does it, not you, and not the owner, who never closes one by hand. `blocked` and `interrupted` leave it open on purpose. **Caps** come from the plan; on a cap hit, stop and report `interrupted`.

## 6. Rules for every task

- **Investigate before writing.** Read the file, check the reference, confirm assumptions. Measure compiled output before fixing cascade/layout bugs (W020).
- **Verify compiled output, not source.** `dist/` HTML for JSON-LD (`<script type="application/ld+json">` has no space before `>` — W047), hreflang, og:image, noindex; `dist/_astro/*.css` for rule order and specificity (W019, W033); page count.
- **Two halves of head-slot data:** frontmatter variables AND the `<Fragment slot="head">` emitting them (W046).
- **Bilingual discipline (W023, W025, W056).** Every user-facing string via `translations.js` + `t(lang, key)`, keys in both `en` and `ar`; every page has an `/ar/` mirror unless scoped EN-only; EN/AR mirrors diffed programmatically (W051). **Arabic never blocks a merge (W125, owner 2026-09-04 — this AMENDS W025).** AR strings you draft ship once the strategist has approved them, which the chunk plan is where it says so; the native reviewer does a **batched** pass afterwards, tracked by a standing ROADMAP row. List every string you shipped in the PR body, with the questions it raises. THE BAR's clause is now "an Arabic string **not approved by the strategist** ships". Company name exact: `شركة الجحجاح التجارية`; `JAHJAH` stays Latin (W022). AR `<title>` suffix uses `companyNameAr`.
- **GROQ:** no function calls across a reference traversal — fetch `_id`, filter on `_ref` (W048).
- **One thing at a time.** No unrelated fixes in a task, no pre-emptive refactors, no "cleanup".
- **Copy a production pattern verbatim** when one exists; adapt data, not structure (W050).
- **Low ceremony output.** State what changed, the hash, the verification. No preambles, no emojis.
- **Secrets:** `.env*` and the VPS env file are read by the process, never by you. Verify by variable *names*. A value seen anywhere outside its store is burned — report it as a finding, do not paste it.
- **Reports and PR bodies are public.** Nothing secret, no unpublished content, no draft copy, no price figures, no customer data, no deploy-hook URL.

## 7. Hard prohibitions

| Never | Why |
|---|---|
| `prerender = false` on a route the plan did not name; `output: 'server'` | Public pages stay static (W074) |
| A price, stock, customer or role field in Sanity | Two stores, one boundary (W075) |
| Prices or hidden products in static HTML/JSON/sitemap | Visibility at build time (W077) |
| Remove `perspective: 'published'` or the drafts GROQ guard | Drafts collide with slugs; pages vanish (W012) |
| Simplify the slug validator | Slug clears on every edit (W013) |
| Collapse the three `/admin` rewrites in `vercel.json` | iOS home-screen 404 (W014) |
| Delete `src/data/products*.js`; re-run `scripts/migrate-*.mjs` | Backups; one-shot migrations |
| Convert `specs` to an object; translate brand names | Templates use `.map()`; brands are Latin (W010) |
| Any Vercel-only API; a Vercel "ignored build step" | Portability (W078); deploy-hook interplay (W087) |
| Token in a client bundle or `PUBLIC_` var; `SANITY_WRITE_TOKEN` outside server env | W079 |
| Push to `master`; force push; merge without green CI + clean reviewer | W084, and now refused by GitHub itself (W100) |
| **Edit or delete the `master-protection` ruleset**; add a bypass actor | It is GATE 2's machine half. A red `ci` is fixed, never bypassed (W100) |
| **Run `claude setup-token`** | The subscription token is the owner's to create and rotate; the executor never mints one |
| **Merge a Dependabot PR as it stands** | Dependabot is a notifier, not an author (W114). Apply the update yourself in a chunk PR — with secrets present and the change reviewed like any other — then close the bot's PR with a comment naming that PR. A major version needs its own named chunk |
| **Put anything of ours in the user-level `~/.claude`**, or register a hook anywhere but the project | `/root/.claude` exists and is the harness's own (cache, history, projects) — the rule is that *we* add nothing there. A user-level hook is shared infrastructure across every session on the box, and a broken one refuses **Bash**, which is what its own documented kill switch needs (W107, W115). Project level only |
| **Install or edit a hook without `bash -n` first**, or edit one in place | An unparseable hook took out a sister session for eight minutes and the way back was a file tool, not the switch (W107). Validate, then replace atomically |
| **Work directly in another repo's clone** | Cross-repo work is an isolated clone plus a PR — never a push from, or an edit inside, a clone another lane is running out of (W115) |
| Tailwind/shadcn/any CSS framework; React/Vue/Svelte on pages; TS in existing `.astro`/`utils` | W003, W004, W086 |
| Commit `.env*`; print a secret; put anything private in the relay | Relay is world-readable |

## 8. Pitfalls (universal — module traps go to `docs/DECISIONS.md`)

| Symptom | Cause → fix |
|---|---|
| Build: route conflicts with a higher-priority route | Drafts fetched → confirm both guards (W012) |
| Build: "Could not resolve '../X.astro'" | Filename case in import → match exactly; `git mv` via temp name (W016) |
| Scoped RTL rule outranks a state rule | Astro scope injection on leading attribute selectors → chain on `html[dir="rtl"]`; check `dist/_astro/*.css` (W019) |
| "Wrong" rule wins at equal specificity | Source order → shared rules first (W033) |
| `Astro.url.pathname` wrong in `404.astro` | Build-time value → client-side `is:inline` detection (W034) |
| Studio slug clears on edit | Validator reverted → restore (W013) |
| Live shows old content after publish | Static staleness / browser cache → check the newest Vercel deployment, hard-refresh (W039) |
| 0 JSON-LD blocks after adding data | Missing `<Fragment slot="head">` (W046) |
| `grep -c` undercounts | Counts lines, not matches → `grep -o | wc -l` |
| Studio "Tool not found: admin" | `basePath: '/admin'` missing in `sanity.config.ts` |
| Non-unique `_key` warnings | Text-derived keys → index-based |

## 9. Commands

```bash
cd /opt/jahjah/web
git fetch && git status -sb && git log --oneline -5
npm ci && npm run build                       # 68 pages built; verify counts 67 CONTENT pages (/admin excluded)
npm run reference                             # regenerate docs/reference/site.md
gh pr create --fill --base master             # after push of a chunk branch
gh pr checks --watch                          # CI
gh pr merge --squash --delete-branch <n> --subject "<subject> (#<n>)"   # FLAGS FIRST — traps below
curl -sI https://jahjah-website.vercel.app/products/          # live probe, ANY path
curl -s https://jahjah-website.vercel.app/ -o /dev/null -w '%{http_code}\n'   # status only
EXPECTED_PAGES=67 bash scripts/verify.sh      # the skill's canonical spelling
```

**These were measured from a dispatched (`claude -p`) session, not assumed** — six probes on
2026-09-03, before and after the allowlist repair. Before it, **9 of 10** commands a dispatched
session needs were refused, two of them named in §5's own preflight (`gh auth status`,
`git ls-remote`). **Not every line above was probed:** `cd` (which needs no rule), `git fetch`,
`git status`, `git log`, `npm ci`, `npm run build`, `npm run reference`, `gh pr create`,
`gh pr checks --watch`, and `gh pr merge --squash --delete-branch <n> --subject "…"` — that last one **cannot** be probed without
merging a real pull request, so do not "verify" it destructively. It has since been exercised on three
real merges (#37, #41, #42) and the existing rule fires with the trailing `--subject` in place. `git ls-files` was probed and runs **unruled**, as do `head` and `tail`.
Do not generalize that to "read-only git is auto-approved" — `git ls-remote origin HEAD` is read-only
and was **DENIED**, which is exactly why its rule exists.

**PASS `--subject` WHENEVER THE MERGE COMMIT SUBJECT MATTERS, and `master` cannot be rewritten to fix
it.** Measured on PR #34: the PR title was edited before merging and confirmed correct, `gh pr merge
--squash` was run without `--subject`, and GitHub took **the branch's own first commit message**
instead — subject *and* body. `master` now permanently carries a commit reading "…[AR: pending native
review] (#34)" over a body opening "THIS PR IS NOT PRE-AUTHORIZED AND MUST NOT BE MERGED", both false
as of the owner's ruling that day, and both in the Vercel deployment metadata too. Editing the PR
title is not enough. #37, #41 and #42 were merged with `--subject` and are correct.

**Five traps, each measured:**

- **A `:*` rule breaks at a token boundary.** `Bash(curl -sI https://…vercel.app:*)` did not match
  `…vercel.app/products/` — appending path is not "an argument". Rules that must accept a path end
  `/*`, and the two origin-only rules were deleted once the `/*` forms were measured to cover both
  `/` and `/products/`.
- **A leading assignment is part of the command, and a rule must not stop at it.**
  `EXPECTED_PAGES=67 bash scripts/verify.sh` does not match `Bash(bash scripts/verify.sh:*)`. The
  fix is the **literal** rule `Bash(EXPECTED_PAGES=67 bash scripts/verify.sh:*)`. **That pins 67 in a
  second place:** if `EXPECTED_PAGES` moves in `ci.yml`, move it here in the same PR or the canonical
  verify spelling is silently refused in the next dispatched chunk. **It is not
  `Bash(EXPECTED_PAGES=*)`** — that was written in this chunk and proved to be a universal bypass:
  `gh workflow list` was DENIED bare and **RAN** when prefixed `EXPECTED_PAGES=1`. Every deny pattern
  starts at token 1, so nothing catches such a command. **A rule ending at an assignment matches
  every command carrying that prefix and voids the deny list behind eight characters.**
- **Flag order decides whether a rule fires.** `gh pr merge <n> --squash --delete-branch` would need a
  new wide rule (which would also permit `--admin`); `gh pr merge --squash --delete-branch <n>`
  matches the existing narrow one. Likewise `curl -s <url> -o /dev/null -w …` matches a URL-first
  rule. **Put the constrained token first** — but know what that buys: a prefix rule scopes only the
  FIRST url, so a second URL later on the same line is still permitted. Host scoping is **not**
  expressible in this syntax, and no rule here achieves it.
- **A pipeline is probably validated segment by segment** — inference, not measurement: `curl … |
  head -1` runs, but `head` auto-approves, so nothing in that probe was deniable. Recorded because it
  is the safe belief to hold; with `head` and `tail` both unruled it changes nothing today.
- **A WORKING-DIRECTORY SANDBOX SITS ABOVE THE ALLOW LIST, AND NO RULE OVERRIDES IT.** Both
  `mkdir -p /tmp/x` and `printf … > /tmp/x.md` are refused: *"Claude Code may only write to files in
  the allowed working directories for this session: '/opt/jahjah/web'"*. A rule written for it was
  proved unable to fire and removed rather than shipped. Same class as W103(a).

> **⚠ WRITE THE REPORT BODY INSIDE THE REPO.** `/relay-report`'s own example path was
> `/tmp/relay-report.md`, and **that write is refused** — measured above. The refusal is scoped to the
> session's working directory, so assume it applies to **any session rooted in `/opt/jahjah/web`**,
> dispatched or interactive; nothing has measured an exemption. Use an absolute repo-internal path —
> `/opt/jahjah/web/.astro/relay-report.md` — and pass that to the publish wrapper. The skill now
> carries this path.
> Scratch files generally: inside the repo (`mkdir` there needs no rule) and deleted afterwards, or
> written to `.astro/`. Asset download works the same way and is measured:
> `curl -sSL https://cdn.sanity.io/images/… -o <repo>/.astro/x.webp` (the probe wrote to `.work/`,
> which is **not** gitignored — prefer `.astro/`).

**`node -e` is allowed, and that has a consequence worth stating.** Once any interpreter is allowed,
the deny block's `Read(.env*)` and `Bash(cat .env:*)` lines are advisory rather than a boundary —
W095 says exactly this and names `node -e` in its own list. What holds is §6: a value seen outside
its store is burned and must be reported, never pasted. `npm update`, `gh pr close` and `gh pr list` exist for the
Dependabot flow in §7 (W114), which is their only caller; `gh pr comment` serves that **and** §5's
rule to answer a Codex finding in the PR.

**If a command you need is refused, that is a finding, not an obstacle to route around** — report
it with the exact command. A dispatched session cannot edit `.claude/**` at all (W116) — **and
`.claude/settings.json` in particular has refused every route yet tried, from interactive sessions
too** (ROADMAP F47). The one-line `"Bash(gh issue close:*)"` addition has been attempted and refused
**three times**: twice in P1.2, a scripted edit and the Edit tool alike (W133), in what the P2a plan
records as an `acceptEdits` session — F47 itself says only "interactive"; and a third time by
P2a · T1 · Session A, invoked per its chunk plan in **default** permission mode precisely to test
whether it would prompt instead. **It did not prompt** — and the same words came back every time:
`Permission for this action was denied by the Claude Code auto mode classifier. Reason: Blocked by
classifier.` **What the third attempt does not settle** is which of two readings holds, because a
session cannot read back its own permission mode: either default mode is no protection from the
classifier, or the invocation did not put the session in default mode at all — Session A's own
harness system-reminder said auto mode was active. Either way F47 stands, and the axis this task
set out to close — `acceptEdits` versus default — is still open; a further probe of it needs a chunk
plan that names one, because both the P1.2 ruling and F47 say do not retry this edit. **Every
refusal so far has landed on that one file:** the same Session A edited
`.claude/skills/relay-report/SKILL.md` without complaint, as P1.2 had edited `reviewer.md` and both
skills. Only those few paths have ever been tried, so the boundary is still unmapped and is not
guessed at here. The remedy lies outside a Claude Code session — the owner edits the file by hand,
or the classifier is configured to permit it. Until one of those happens, a plan that turns on
adding an allow rule stops here.
